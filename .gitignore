__pycache__/
*.pyc
*.egg-info/
# uv.lock is not versioned — the project pins deps via `pip install -e` (CLAUDE.md §5)
uv.lock
dist/
build/
.pytest_cache/
.scannerwork/
.vscode/
.coverage
coverage.xml
.menezis/
proto/

# Secrets & local configuration
.env
.env.*
config.ini
*.pem
*.key
*.crt
SITREP-TwinRAG.md

# macOS noise
.DS_Store

# Generated by scripts/build_webui.sh — never commit (regen on every build)
src/twindb_lightrag_memgraph/webui_dist/

# Local OVH deploy assets (Dockerfile/stack/.env live on the server, not versioned)
deploy/ovh-twin/

# Procedure bundle store runtime artefacts (default local path + sidecars:
# .lock, mkstemp temporaries, .corrupt-* quarantines)
twin_procedure_bundles.json
twin_procedure_bundles.json.*
