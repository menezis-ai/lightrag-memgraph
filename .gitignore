__pycache__/
*.pyc
*.egg-info/
# uv.lock is not versioned — the project pins deps via `pip install -e` (AGENTS.md "Install")
uv.lock
dist/
build/
.pytest_cache/
.scannerwork/
.vscode/
.coverage
coverage.xml
.vision-live-artifacts/
.menezis/
proto/

# Secrets & local configuration
.env
.env.*
config.ini
*.pem
*.key
*.crt
SITREP-TwinRAG.md

# macOS noise
.DS_Store

# Generated by scripts/build_webui.sh — never commit (regen on every build)
src/twindb_lightrag_memgraph/webui_dist/

# Local deployment assets (Dockerfile/stack/.env live on the server, not versioned)
deploy/

# Procedure bundle store runtime artefacts (default local path + sidecars:
# .lock, mkstemp temporaries, .corrupt-* quarantines)
twin_procedure_bundles.json
twin_procedure_bundles.json.*

# Mutation testing (mutmut 3.x) — source copy + session state, regenerated per run
mutants/
.mutmut-cache
mutmut-stats.json

# services/* distributions (twin_catalog): local venvs and build artefacts
services/*/.venv/
services/*/*.egg-info/
services/*/build/
services/*/dist/
services/twin_catalog/admin-ui/node_modules/
services/twin_catalog/admin-ui/tsconfig.tsbuildinfo
services/twin_catalog/twin_catalog/admin_dist/

# Local agent memory snapshot (MEMORY.md + feedback_/project_/reference_ notes).
# Operator-local state, not repository content — never versioned.
docs/history/
