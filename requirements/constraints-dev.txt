# CI/dev security floor constraints.
# Keep LightRAG unpinned here so the workflow's explicit
# `lightrag-hku[api]==1.5.6` install (the single supported pin,
# qualified 2026-08-06) always wins.
black>=24.10.0
pre-commit>=4.2.0
pydantic-settings>=2.14.2
pypdf>=6.14.2
python-multipart>=0.0.31
ruff>=0.8.0
starlette>=1.3.1
